import styled from 'styled-components/native';
import Colors from '../../global/colors';

export const Container = styled.View`
  margin-top: 10px;
  border-radius: 2px;
  border: 0.6px solid ${Colors.SUPPORT_200}
`;
